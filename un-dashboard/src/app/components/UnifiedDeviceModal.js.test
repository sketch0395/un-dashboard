import React from 'react';
import Modal from "./Modal";

const UnifiedDeviceModal = ({ 
    modalDevice, 
    setModalDevice, 
    onSave,
    onStartSSH,
    systemUptime 
}) => {
    return (
        <Modal isVisible={!!modalDevice} onClose={() => setModalDevice(null)}>
            <div>
                <h2>Test Modal - {modalDevice?.name || 'Device'}</h2>
                <p>IP: {modalDevice?.ip}</p>
                <button onClick={() => setModalDevice(null)}>Close</button>
            </div>
        </Modal>
    );
};

export default UnifiedDeviceModal;
